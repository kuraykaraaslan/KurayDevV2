import * as MailService from '@/services/NotificationService/MailService'

describe('NotificationService - MailService', () => {
  it('should be defined', () => {
    expect(MailService).toBeDefined()
  })
})
