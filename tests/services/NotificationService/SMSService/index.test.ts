import * as SMSIndex from '@/services/NotificationService/SMSService'

describe('NotificationService - SMS - index', () => {
  it('should be defined', () => {
    expect(SMSIndex).toBeDefined()
  })
})
