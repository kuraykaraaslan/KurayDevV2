import * as GoogleService from '@/services/AuthService/SSOService/GoogleService'

describe('AuthService - SSO - GoogleService', () => {
  it('should be defined', () => {
    expect(GoogleService).toBeDefined()
  })
})
