import * as GHService from '@/services/AuthService/SSOService/GithubService'

describe('AuthService - SSO - GithubService', () => {
  it('should be defined', () => {
    expect(GHService).toBeDefined()
  })
})
