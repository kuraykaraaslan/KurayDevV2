import * as SlackService from '@/services/AuthService/SSOService/SlackService'

describe('AuthService - SSO - SlackService', () => {
  it('should be defined', () => {
    expect(SlackService).toBeDefined()
  })
})
