import * as TwitterService from '@/services/AuthService/SSOService/TwitterService'

describe('AuthService - SSO - TwitterService', () => {
  it('should be defined', () => {
    expect(TwitterService).toBeDefined()
  })
})
