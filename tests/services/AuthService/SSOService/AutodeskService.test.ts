import * as AutodeskService from '@/services/AuthService/SSOService/AutodeskService'

describe('AuthService - SSO - AutodeskService', () => {
  it('should be defined', () => {
    expect(AutodeskService).toBeDefined()
  })
})
