import * as TiktokService from '@/services/AuthService/SSOService/TiktokService'

describe('AuthService - SSO - TiktokService', () => {
  it('should be defined', () => {
    expect(TiktokService).toBeDefined()
  })
})
