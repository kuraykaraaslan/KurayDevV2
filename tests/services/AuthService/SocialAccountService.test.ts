import * as SocialAccountService from '@/services/AuthService/SocialAccountService'

describe('AuthService - SocialAccountService', () => {
  it('should be defined', () => {
    expect(SocialAccountService).toBeDefined()
  })
})
